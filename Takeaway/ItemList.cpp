#include "ItemList.h"
vector<Item*>items;
vector<Item*> ItemList::getVector() { // function implementation that gets the item vector from the items list 
	return items; // returns the items vector
}
string toString() {
	string random;
	return random;
}