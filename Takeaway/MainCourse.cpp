#include "MainCourse.h"
MainCourse::MainCourse(string aName, float aPrice, int aCalories) {
		name = aName; // Item class name = aName thats called in the main course function
		price = aPrice;
		calories = aCalories;
}